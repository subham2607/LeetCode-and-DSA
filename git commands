git init
git status
git add .
git commit -m "New project to push to Github
git remote add origin https://github.com/subham2607/LeetCode-and-DSA.git
git push -u -f origin master
git pull origin



This is end

For Update :- update in code then below steps

git add .
git commit -m 
git push
